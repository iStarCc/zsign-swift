# minizip

来自 [zlib contrib/minizip](https://github.com/madler/zlib/tree/master/contrib/minizip)。

用于 IPA 解压与打包，与 zsign 使用相同的 minizip 实现，确保生成的 IPA 与 iOS 安装工具兼容（显式目录条目、deflate 压缩等）。

## 许可

遵循 zlib 许可，与 zlib 相同。
